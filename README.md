# RSTT


Author / Publisher:

Helios

RSTT is a completely local-first, free, privacy-focused real-time speech-to-text application for Windows.

The application captures audio being played by Windows, converts speech to text in real time using a completely local open-source speech recognition model, displays live captions, and optionally types stable recognised text into whichever application currently has keyboard focus.